# agent_poc

This agent creates a PR review ticket when a new pull request is created.

Implemented
- Automatically creates a review ticket for a newly opened pull request.

Behavior
- On creation of a new pull request, the agent generates a corresponding PR review ticket.

```
